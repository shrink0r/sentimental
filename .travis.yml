language: php

php:
  - 5.6
  - nightly
  - hhvm
  - hhvm-nightly

matrix:
  allow_failures:
    - php: hhvm

install:
  - composer install

script: phpunit tests/unit

after_script: php vendor/bin/coveralls -v
